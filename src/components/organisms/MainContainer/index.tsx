import * as React from 'react'
import * as style from './style.css'

const MainContainer = (props: any) => (
  <main className={style.mainContainer} {...props}>
  </main>
)

export default MainContainer
